---
title: "Google Gemini"
date: "2026-05-13T00:55:20.923Z"
url: "https://gemini.google.com/app"
platform: "gemini"
message_count: 4
---

# Google Gemini (4 Messages)

🔗 **Original**: [https://gemini.google.com/app](https://gemini.google.com/app)
📅 **Exported at**: 2026-05-13T00:55:20.923Z

> **Statistics**
> - **Messages**: 4 (2 User, 2 Assistant)
> - **Word Count**: ~402 (~2 min read)
> - **Code Blocks**: 4


---

<a id="message-1"></a>
## 👤 User
*🕐 2026-05-13T00:55:20.923Z | #1 | Turn 1*

> คุณบอกว่า
>
> บน Windows ผมจะสามารถใช้คำสั่ง shortcut แบบเดียวกับที่ผมใส่ไว้ใน ~/.bashrc ของ linux ได้ด้วยวิธีใด

---

<a id="message-2"></a>
## 🤖 gemini
*🕐 2026-05-13T00:55:20.923Z | #2*

การใช้งานคำสั่งลัด (Aliases) บน Windows เพื่อให้ได้ฟีเจอร์เหมือนกับ `~/.bashrc` ใน Linux นั้นทำได้หลายวิธี ขึ้นอยู่กับว่าคุณใช้โปรแกรม Terminal ตัวไหนเป็นหลักครับ

นี่คือ 3 วิธีที่นิยมที่สุด:

---

## 1\. สำหรับ PowerShell (แนะนำที่สุด)

PowerShell คือหัวใจหลักของ Windows ยุคใหม่ ไฟล์ที่เทียบเท่ากับ `.bashrc` จะเรียกว่า **PowerShell Profile**

1.  เปิด PowerShell แล้วพิมพ์คำสั่งเพื่อดูว่ามีไฟล์โปรไฟล์อยู่หรือยัง: `if (!(Test-Path $PROFILE)) { New-Item -Type File -Path $PROFILE -Force }`
    
2.  เปิดไฟล์นั้นด้วย Notepad: `notepad $PROFILE`
    
3.  ใส่คำสั่งแบบนี้ลงไป (ใช้ Keyword `function` จะยืดหยุ่นกว่า alias ปกติ):
    
    ```

    function gs { git status }
    function ll { ls -l }
    function .. { cd .. }
    
    ```

    
4.  บันทึกไฟล์แล้วปิด Notepad จากนั้นรันคำสั่งนี้เพื่อให้เริ่มทำงานทันที: `. $PROFILE`
    

---

## 2\. สำหรับ Command Prompt (cmd.exe)

CMD ไม่มีไฟล์ config อัตโนมัติเหมือน Bash แต่เราสามารถสร้าง "AutoRun" ขึ้นมาเองได้

1.  สร้างไฟล์ชื่อ `alias.bat` ไว้ในโฟลเดอร์ที่คุณหาเจอง่ายๆ (เช่น `C:\tools\alias.bat`)
    
2.  ใส่คำสั่งลงไปในรูปแบบ `doskey`:
    

    ```

    @echo off
    doskey gs=git status
    doskey ls=dir /w
    doskey ..=cd ..
    ```

3.  ตั้งค่าให้ CMD รันไฟล์นี้ทุกครั้งที่เปิดโปรแกรม:
    *   กด `Win + R` พิมพ์ `regedit`
    *   ไปที่: `HKEY_CURRENT_USER\Software\Microsoft\Command Processor`
    *   คลิกขวา New -> **String Value** ชื่อว่า `AutoRun`
    *   ใส่ที่อยู่ไฟล์ลงไปใน Value data: `C:\tools\alias.bat`

---

## 3. สำหรับ Git Bash (ถ้าคุณลงไว้)
หากคุณติดตั้ง **Git for Windows** คุณจะได้สภาพแวดล้อมที่แทบจะเหมือน Linux 100%

*   คุณสามารถไปที่โฟลเดอร์ผู้ใช้ของคุณ (`C:\Users\YourName`)
*   สร้างไฟล์ชื่อ `.bashrc` หรือ `.bash_profile`
*   ใส่ Alias แบบ Linux ปกติได้เลย:
    `alias gs='git status'`

---

### สรุปการเลือกใช้

| ความต้องการ | วิธีที่แนะนำ |
| :--- | :--- |
| **เน้นใช้งานมาตรฐาน Windows** | PowerShell Profile |
| **เน้นความคุ้นเคยเหมือน Linux** | Git Bash |
| **ใช้งานแบบ Legacy เก่าๆ** | CMD (Doskey) |

> **ข้อควรระวัง:** สำหรับ PowerShell หากรันไม่ได้ในครั้งแรก คุณอาจต้องเปิดสิทธิ์การรัน Script ก่อนโดยพิมพ์คำสั่ง (ในฐานะ Admin): `Set-ExecutionPolicy RemoteSigned` ครับ

ปกติแล้วคุณเน้นเขียนโปรแกรมหรือใช้งานด้านไหนเป็นพิเศษไหมครับ? ผมจะได้แนะนำการตั้งค่าที่เหมาะกับงานนั้นให้มากขึ้น

```

---


*Generated by [NousSave Ai Chat Exporter](https://www.noussave.com) | gemini | 5/13/2026*
