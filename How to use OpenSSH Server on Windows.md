---
title: "Google Gemini"
date: "2026-05-16T16:57:23.465Z"
url: "https://gemini.google.com/app"
platform: "gemini"
message_count: 6
---

# Google Gemini (6 Messages)

🔗 **Original**: [https://gemini.google.com/app](https://gemini.google.com/app)
📅 **Exported at**: 2026-05-16T16:57:23.465Z

> **Statistics**
> - **Messages**: 6 (3 User, 3 Assistant)
> - **Word Count**: ~325 (~1 min read)
> - **Code Blocks**: 6


---

## Table of Contents

1. [User: คุณบอกว่า จะ download openssh server ของ windows...](#message-1)
2. [Assistant: การติดตั้ง OpenSSH Server บน Windows 10 ในปัจจุบ...](#message-2)
3. [User: คุณบอกว่า เมื่อสั่ง Start-Service -sshd ขึ้น err...](#message-3)
4. [Assistant: ขออภัยด้วยครับ! เกิดความผิดพลาดในคำสั่ง PowerShe...](#message-4)
5. [User: คุณบอกว่า หลังจากทำทั้งสามคำสั่งสำเร็จแล้วทุกครั...](#message-5)
6. [Assistant: ไม่ต้องทำใหม่แล้วครับ! หลังจากที่คุณรัน 3 คำสั่ง...](#message-6)

<a id="message-1"></a>
## 👤 User
*🕐 2026-05-16T16:57:23.465Z | #1 | Turn 1*

> คุณบอกว่า
>
> จะ download openssh server ของ windows 10 ทำอย่างไร

---

<a id="message-2"></a>
## 🤖 gemini
*🕐 2026-05-16T16:57:23.465Z | #2*

การติดตั้ง OpenSSH Server บน Windows 10 ในปัจจุบันทำได้ง่ายมากครับ เพราะ Microsoft ได้รวมมันเข้าเป็น **ฟีเจอร์เสริม (Optional Feature)** ของระบบปฏิบัติการเรียกว่าไม่ต้องไปหาลิงก์ดาวน์โหลดจากเว็บเถื่อนให้เสี่ยงเลยครับ

คุณสามารถเลือกติดตั้งได้ 2 วิธีหลักๆ ตามความถนัด ดังนี้ครับ:

---

## วิธีที่ 1: ติดตั้งผ่านหน้า Settings (ง่ายที่สุด)

วิธีนี้เหมาะสำหรับสายคลิก ใช้งานง่ายผ่านหน้าต่าง Windows ปกติ

1.  กดปุ่ม **Start** (โลโก้ Windows) แล้วคลิกที่รูปฟันเฟืองเพื่อเข้า **Settings** (หรือกดปุ่มลัด `Windows + I`)
    
2.  เลือกไปที่หัวข้อ **Apps** จากนั้นคลิกที่ **Optional features** (คุณสมบัติเสริม)
    
3.  คลิกที่ปุ่ม **Add a feature** (เพิ่มคุณสมบัติ) ที่อยู่ด้านบน
    
4.  ในช่องค้นหา ให้พิมพ์คำว่า **`OpenSSH`**
    
5.  ติ๊กถูกที่ช่อง **OpenSSH Server** แล้วกดปุ่ม **Install**
    
6.  รอระบบดาวน์โหลดและติดตั้งสักครู่ เมื่อเสร็จแล้วจะขึ้นสถานะ Installed ครับ
    

---

## วิธีที่ 2: ติดตั้งผ่าน PowerShell (เร็วที่สุด)

ถ้าคุณชอบความรวดเร็วแบบสายโค้ด วิธีนี้ตอบโจทย์ครับ

1.  คลิกขวาที่ปุ่ม **Start** แล้วเลือก **Windows PowerShell (Admin)** หรือ **Terminal (Admin)**
    
2.  คัดลอกคำสั่งด้านล่างนี้ไปวางแล้วกด Enter เพื่อเช็คและติดตั้ง OpenSSH Server:
    

```

Add-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0

```

> **ข้อสังเกต:** ถ้าระบบขึ้นว่า `Online : True` และ `RestartNeeded : False` แปลว่าติดตั้งสำเร็จเรียบร้อยครับ
---

## ขั้นตอนสำคัญ: การเปิดใช้งาน (Start Service)

หลังจากดาวน์โหลดและติดตั้งเสร็จแล้ว ตัว Server จะยังไม่ทำงานทันทีและจะไม่เปิดเองตอนเปิดคอม (ค่านิยมเริ่มต้นจะเป็นแบบ Manual) เราต้องไปเปิดและตั้งค่าให้มันรันอัตโนมัติครับ โดยใช้ **PowerShell (Admin)** ตัวเดิมพิมพ์คำสั่งต่อไปนี้:

```

# 1. ตั้งค่าให้ OpenSSH Server เปิดใช้งานอัตโนมัติทุกครั้งที่เปิดเครื่อง
Set-Service -Name sshd -StartupType 'Automatic'

# 2. สั่งให้ OpenSSH Server เริ่มทำงานทันทีในตอนนี้
Start-Service -sshd

```

### เช็คความพร้อมใช้งาน

คุณสามารถตรวจสอบว่าตัว Server ทำงานอยู่หรือไม่ โดยใช้คำสั่ง:

```

Get-Service -Name sshd

```

ถ้าขึ้นสถานะ **`Running`** แปลว่าตอนนี้ Windows 10 ของคุณพร้อมเปิดรับการเชื่อมต่อจากภายนอกผ่าน SSH แล้วครับ (อย่าลืมเช็คเรื่อง Firewall พอร์ต 22 หากต้องการเชื่อมต่อจากคอมพิวเตอร์เครื่องอื่นนอกเครือข่ายด้วยนะ)

---

<a id="message-3"></a>
## 👤 User
*🕐 2026-05-16T16:57:23.465Z | #3 | Turn 2*

> คุณบอกว่า
>
> เมื่อสั่ง Start-Service -sshd ขึ้น error ว่า A parameter cannot be found that matches parameter name 'sshd'.

---

<a id="message-4"></a>
## 🤖 gemini
*🕐 2026-05-16T16:57:23.465Z | #4*

ขออภัยด้วยครับ! เกิดความผิดพลาดในคำสั่ง PowerShell ที่ผมให้ไปก่อนหน้านี้เล็กน้อยครับ

ความผิดพลาดเกิดจากผมพิมพ์เครื่องหมายขีดลบ (`-`) เกินมาหน้าคำว่า `sshd` ทำให้ PowerShell เข้าใจผิดว่า `sshd` คือชื่อพารามิเตอร์ (Parameter) ไม่ใช่ชื่อบริการ (Service Name) ครับ

วิธีแก้ไขง่ายมากครับ ให้ใช้คำสั่งที่ถูกต้องตามนี้ใน **PowerShell (Admin)** ได้เลยครับ:

```

Start-Service -Name sshd

```

*(หรือจะพิมพ์สั้นๆ แค่ `Start-Service sshd` โดยไม่ต้องมี `-Name` ก็ได้เช่นกันครับ)*

---

### แถมท้าย: มัดรวมคำสั่งเปิดใช้งานที่ถูกต้อง

เพื่อความชัวร์ คุณสามารถก๊อปปี้ชุดคำสั่งที่ถูกต้องด้านล่างนี้ไปวางใน PowerShell (Admin) เพื่อตั้งค่าให้เสร็จสรรพได้เลยครับ:

```

# 1. ตั้งค่าให้ OpenSSH Server เปิดใช้งานอัตโนมัติทุกครั้งที่เปิดเครื่อง
Set-Service -Name sshd -StartupType 'Automatic'

# 2. สั่งให้ OpenSSH Server เริ่มทำงานทันที
Start-Service -Name sshd

# 3. ตรวจสอบสถานะการทำงาน (ต้องขึ้นว่า Running)
Get-Service -Name sshd

```

เมื่อสั่งคำสั่งที่ 3 แล้วเห็นสถานะเป็น **Running** สีเขียว (หรือตัวอักษรปกติที่ระบุว่า Running) แปลว่าเรียบร้อยพร้อมใช้งานแล้วครับ!

---

<a id="message-5"></a>
## 👤 User
*🕐 2026-05-16T16:57:23.465Z | #5 | Turn 3*

> คุณบอกว่า
>
> หลังจากทำทั้งสามคำสั่งสำเร็จแล้วทุกครั้งที่ Boot เครื่องมาใหม่ ต้องทำคำสั่งเหล่านี้ใหม่หรือไม่

---

<a id="message-6"></a>
## 🤖 gemini
*🕐 2026-05-16T16:57:23.465Z | #6*

**ไม่ต้องทำใหม่แล้วครับ!** หลังจากที่คุณรัน 3 คำสั่งนั้นสำเร็จเป็นที่เรียบร้อยแล้ว ทุกครั้งที่เปิดเครื่อง (Boot) ใหม่ ระบบ Windows 10 จะจัดการเปิดบริการ OpenSSH Server ให้คุณโดยอัตโนมัติทันทีครับ

เหตุผลที่เป็นแบบนั้นเพราะคำสั่งนี้ครับ:

> `Set-Service -Name sshd -StartupType 'Automatic'`

คำสั่งนี้คือการเปลี่ยนสถานะของตัวบริการ (Service) ให้เป็น **"Automatic"** ซึ่งหมายความว่า **"ให้ Windows สั่งรันบริการนี้เองทุกครั้งที่ระบบเปิดขึ้นมา"** ดังนั้นคุณจึงไม่ต้องมานั่งเปิด PowerShell เพื่อพิมพ์คำสั่งใหม่ทุกครั้งที่เปิดคอมพิวเตอร์ครับ

---

### วิธีทดสอบเพื่อความสบายใจ

หากคุณอยากมั่นใจว่ามันเปิดเองจริงไหม หลังจากที่คุณรีสตาร์ทเครื่องคอมพิวเตอร์ครั้งถัดไป ให้ลองเปิด PowerShell ขึ้นมา (ไม่ต้องกด Run as Administrator ก็ได้) แล้วพิมพ์คำสั่งเช็คสถานะสั้นๆ:

```

Get-Service sshd

```

ถ้าเห็นสถานะขึ้นว่า **`Running`** ทันทีหลังจากบูตเครื่อง โดยที่คุณยังไม่ได้สั่ง `Start-Service` เลย แปลว่าระบบทำงานได้ถูกต้องสมบูรณ์แบบ 100% แล้วครับ ซอฟต์แวร์นี้จะฝังตัวเป็นเบื้องหลัง (Background Service) พร้อมให้คุณเชื่อมต่อเข้ามาได้ตลอดเวลาตราบใดที่เปิดคอมพิวเตอร์ทิ้งไว้ครับ

---


*Generated by [NousSave Ai Chat Exporter](https://www.noussave.com) | gemini | 5/16/2026*
